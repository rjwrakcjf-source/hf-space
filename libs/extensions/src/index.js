const { ExtensionLoader } = require('./loader/extension-loader');
const { ExtensionManager } = require('./manager/extension-manager');
const { MarketplaceClient } = require('./marketplace/marketplace-client');

module.exports = { ExtensionLoader, ExtensionManager, MarketplaceClient };
